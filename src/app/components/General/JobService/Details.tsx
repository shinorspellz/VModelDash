import React from "react";
import VMModal from "../Widget/VMModal";

const Details = ({ metaInfo }: any) => {
  return (
    <>
      <VMModal url="/jobs" backTitle="Jobs & Services"></VMModal>
    </>
  );
};

export default Details;
